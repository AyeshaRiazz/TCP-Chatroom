# TCP-Chatroom
A TCP Chatroom is a network-based communication system that enables multiple clients to connect to a central server and exchange messages in real time. It uses the TCP protocol to ensure reliable and ordered delivery of messages between users. This project demonstrates socket programming.
